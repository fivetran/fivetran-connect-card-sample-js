import React from 'react';
import ReactDOM from 'react-dom';
import PBFApp from './PBF-App';

ReactDOM.render(
  <React.StrictMode>
    <PBFApp />
  </React.StrictMode>,
  document.getElementById('root')
);

